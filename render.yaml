services:
  - type: web
    name: atlas-agent
    runtime: python3
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn multi_agent.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
